---
name: bug_report
about: Toolshed behaves unexpectedly (routing, recovery, errors)
title: ''
labels: bug
assignees: ''
---

**Hermes version / commit:**
(e.g. v0.20.5 / b766607b — see `hermes --version`)

**Toolshed version / commit:**
(see `__about__.py` in the installed plugin directory)

**OS:**
(distro + version)

**Profile count:**
(how many Hermes agents/profiles run on this machine?)

**Install method:**
(`plugins install owner/repo`, `--ref <sha>`, local path, …)

**doctor output:**
(paste if available)

**Expected behavior:**

**Actual behavior:**

**Steps to reproduce:**
1.
2.
3.

**Logs:**
(relevant lines from `~/.hermes/profiles/<profile>/logs/agent.log` — grep for
`token-router|narrowed to|request_toolset`. Remove anything private.)
